import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  root: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },  
  lottieAnimation: {
    width: '100%',
    height: '100%',
  },
  TextVersion: {
    position:"absolute",
    bottom:30,
    fontWeight:'bold'
  }
  
});
